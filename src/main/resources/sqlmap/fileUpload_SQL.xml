<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.brownie.fileUpload.service.impl.FileMapper">

	<insert id="insertPath" parameterType="hashMap">
		INSERT INTO 
		    BROWNIE_FILE
		    (
		    FILE_SEQ, SUB_SEQ, 
		    FILE_PATH, ORIGINAL_NAME,
		    SAVE_NAME, IN_DATE,
		    IN_USER_ID, UP_DATE, 
		    UP_USER_ID
			)
			values
			(
			    FILE_SEQ.nextval, SUB_SEQ.nextval,
			    '${profilePath}', '${originFileName}',
			    '${savefileName}', SYSDATE,
			    '${id}', SYSDATE,
			    '${id}'
			)
	</insert>
</mapper>